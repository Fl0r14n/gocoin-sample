'use strict';
var app = angular.module('app', ['controller']);

var controllers = angular.module('controller', []);

controllers.controller('BitcoinController', ['$scope', function($scope) { 
        
}]);

controllers.controller('LitecoinController', ['$scope', function($scope) { 
        
}]);

controllers.controller('DogecoinController', ['$scope', function($scope) { 
        
}]);
